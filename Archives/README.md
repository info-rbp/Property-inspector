# Archives

This directory is reserved for storing ZIP archives of the original applications. Add your `.zip` files here to keep them versioned alongside the project.

If the folder is empty, the accompanying `.gitkeep` file ensures the directory remains in the repository.
